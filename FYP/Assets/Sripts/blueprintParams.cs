using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class blueprintParams : MonoBehaviour {

   /*
	    private float minLength;
        private float maxLength;
        private float minWidth;
        private float maxWidth;
        private float floorHeight;
        private float settleCycles = 5;
        private float minFilledPercent = 10;
        private float minDoorDist = 3;

        public blueprintParams(float _minLength, float _maxLength, float _minWidth, float _maxWidth, float floorHeight, float settleCycles,*/


}
